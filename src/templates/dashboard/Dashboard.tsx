import { Button } from "react-bootstrap";

const DashboardTemplate = (): JSX.Element => {
  return (
    <div>
      <span> Next-Ts-Template-Dashboard</span>
      <Button>Click to redux me</Button>
    </div>
  );
};
export default DashboardTemplate;
